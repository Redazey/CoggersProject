# Используй официальный образ Node.js
FROM node:latest

# Установи рабочую директорию
WORKDIR /usr/src/app

# Скопируй package.json и package-lock.json в рабочую директорию
COPY package*.json ./

# Установи зависимости
RUN npm install

# Скопируй все файлы из директории с билдом в рабочую директорию
COPY . .

# Определи порт, на котором будет работать веб-сайт
EXPOSE 5137

# Запусти приложение при старте контейнера
CMD ["npm", "run", "dev"]
